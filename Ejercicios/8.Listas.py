lista = ['a', 'b', 'c']
lista.append('d')
# tamaño de la lista e imprimir cada elemento
for i in range(len(lista)):
    print(lista[i])

# separar la lista en elementos
for elemento in lista:
    print(elemento)
